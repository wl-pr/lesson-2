name: Ansible Playbook
on:
  push:
    branches:
      - master

jobs:
  run-playbooks:
    runs-on: ubuntu-latest  
    steps:
    - name: Run Ansible Playbook
      uses: actions/checkout@v1
      with:
       playbook: play.yml
       key: ${{secrets.KEY}}
       inventory: |
         [52.59.251.179]
