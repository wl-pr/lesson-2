name: Ansible deploy

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - name: run exporters playbook
      uses: actions/checkout@v1
      with:
        playbook: play.yml
        key: ${{secrets.KEY}}
        inventory: |
          [52.59.251.179]
